@page "/newentry"
@attribute [StreamRendering]

<PageTitle>New Entry</PageTitle>

<h1>Add New Entry</h1>

<div style="display: grid; grid-template-columns: 1.5fr 1fr 1fr 1fr 1fr 1fr; gap: 5px; align-items: center; width: 100%; box-sizing: border-box;">
  <!-- Titles Row -->
  <h3 style="margin: 0; text-align: center; ">Flowers</h3>
  <h3 style="margin: 0; text-align: center; ">Units</h3>
  <h3 style="margin: 0; text-align: center; ">Cost</h3>
  <h5 style="margin: 0; text-align: center; "><u>Date</u></h5>
  <h5 style="margin: 0; text-align: center; "><u>Stem Cost</u></h5>
  <h5 style="margin: 0; text-align: center; "><u>Change Since Last Entry</u></h5>

  <!-- Row 1 -->
  <select>
     @foreach (var flower in @getFlowerOptions())
        {
            <option value="@flower">@flower</option>
        }
  </select>
  <input type="number" placeholder="Units">
  <input type="number" placeholder="Cost">
  <input type="date" style="margin-left: 40%;">
  <h6 style="text-align: center; margin-right: 5%;">0.43</h6>
  <h6 style="text-align: center; margin: 0;">3%</h6>

  <!-- Row 2 -->
  <select>
    <option>Carolina Sapphire</option>
    <option>Veronica</option>
    <option>More</option>
  </select>
  <input type="number" placeholder="Units">
  <input type="number" placeholder="Cost">
  <input type="date"style="margin-left: 40%;">
  <h6 style="text-align: center; margin-right: 5%;">4.83</h6>
  <h6 style="text-align: center; margin: 0;">12%</h6>
</div>

<!-- New Line Button -->
<div style="margin-top: 10px; text-align: left;">
  <button style="background-color: #eb3a9b; color: white; padding: 10px 20px; border: none; border-radius: 5px; font-size: 16px;">New Line</button>
</div>

<!-- Save Button -->
<div style="margin-top: 20px; text-align: left;">
  <button style="background-color: #eb3a9b; color: white; padding: 10px 20px; border: none; border-radius: 5px; font-size: 16px;">Save</button>
</div>


@code{
   public string ReadTxt()
    {
         List<string[]> csvData2 = new List<string[]>();
        //change this as needed
        string filePath = "C:\\Users\\efren\\OneDrive\\Desktop\\FlowerProjectData.txt";


        using (StreamReader reader = new StreamReader(filePath)){
                string line;
                while ((line = reader.ReadLine()) != null){
                    string[] fields = line.Split(',');
                    csvData2.Add(fields);
                }
            }

        foreach (var row in csvData2)
        {
            return string.Join(",", row) + "\n";
        }
        return "";
    }

 public List<string> GetAllFlowers(string data)//works
    {

        int startIndex2 = data.IndexOf("<Flower>");
        int endIndex2 = data.IndexOf("</Flower>") + "</Flower>".Length; // End after </Object>
        string usingData = data;
        
    
        usingData = data.Substring(startIndex2, endIndex2 - startIndex2);

        List<string> flowerNames = new List<string>();
        int startIndex = 0;
        
        while ((startIndex = usingData.IndexOf("<", startIndex)) != -1)
        {
            int endIndex = usingData.IndexOf(">", startIndex);
            if (endIndex == -1) break;

            string tag = usingData.Substring(startIndex + 1, endIndex - startIndex - 1);
            
            if (!tag.Contains("/") && !tag.Contains("Price") && !tag.Contains("Amount") && !tag.Contains("Purchases") && !tag.Contains("Purchase") && !tag.Contains("Date")&& !tag.Contains("Type") && !tag.Contains("Flower"))
            {
                flowerNames.Add(tag);
            }
            startIndex = endIndex + 1;
        }

        return flowerNames;
    }
    public string[] getFlowerOptions(){
        return GetAllFlowers(ReadTxt()).ToArray();
        
    }
}